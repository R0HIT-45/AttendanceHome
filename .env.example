# Supabase Configuration
# Get these values from your Supabase project dashboard
# https://supabase.com -> Settings -> API

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Environment indicator
VITE_APP_ENV=development

# Note:
# - VITE_SUPABASE_URL: Copy from "Project URL" in Supabase Settings → API
# - VITE_SUPABASE_ANON_KEY: Copy from "anon key" in Supabase Settings → API
# - Use different keys for development and production environments
# - Never commit actual keys to version control (use .env.local for local development)
# - For production, set these as environment variables in your hosting platform:
#   - Vercel: Settings → Environment Variables
#   - Netlify: Site settings → Build & deploy → Environment
#   - Traditional Server: Set in .env or export as shell variables
